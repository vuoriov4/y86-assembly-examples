init:
    irmovq $3, %rsi   # divisor 
    irmovq $6, %rdi   # dividend
main:
    irmovq $0x1,%rcx
    rrmovq %rdi,%rdx
    call divide
    halt
divide:
    addq %rcx,%rax
    subq %rsi,%rdx
    jge divide
    subq %rcx,%rax
    ret
